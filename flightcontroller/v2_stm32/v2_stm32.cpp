#include "v2_stm32.h"

//void get_imu (IMU &imu, struct Offsets *offsets) {
//    imu.update();
    
//    gF.filter(imu.getRotationX() - gyro_roll_cal);
//    gF.filter(imu.getRotationY() - gyro_pitch_cal);
//    gF.filter(imu.getRotationZ() - gyro_yaw_cal);
// = 0
//    flight_data.gx = ( gF.getCurrent() / SCALE_GYRO_250 );
//    flight_data.gy = ( gF.getCurrent() / SCALE_GYRO_250 );
//    flight_data.gz = ( gF.getCurrent() / SCALE_GYRO_250 );
//
//    flight_data.ax = (float) ( (imu.getAccelerationX() - acc_x_cal) / SCALE_ACC_2G );
//    flight_data.ay = (float) ( (imu.getAccelerationY() - acc_y_cal) / SCALE_ACC_2G );
//    flight_data.az = (float) ( (imu.getAccelerationZ() - acc_z_cal) / SCALE_ACC_2G );

//}
